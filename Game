class Game:
    def __init__(self, board: Board, players: list, current_player: Player):
        self.board = board
        self.players
        self.current_player = current_player

    # Initialize the game by creating the board
    def start_game():
        board.display_board()

    def switch_player()

    # Start the game loop
    def play_game():
        while (check_game_over() == False):


    def check_game_over() -> bool

